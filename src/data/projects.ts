import { Project } from '@/types/project';

export const projects: Project[] = [
  {
    slug: 'order-completion-refactoring',
    title: '주문 완료 처리 — 거대 단일 메서드를 Command/Provider 패턴으로 분해하기',
    period: '2025.02 - 2025.09 (7개월)',
    tags: ['Java', 'Spring Boot', 'Command Pattern', '보상 트랜잭션'],
    summary:
      '수십 개 외부 시스템 호출이 얽힌 거대 메서드를 Command/Provider 패턴으로 분해하여, 새로운 결제 수단을 추가할 때 기존 코드 수정 없이 100% 확장이 가능하도록 리팩토링했습니다.',
    sections: {
      summary:
        '단일 거대 메서드로 이루어진 주문 완료 처리를 7개월에 걸친 점진적 리팩토링을 통해 분해했습니다. Command 패턴과 마커 인터페이스 기반 Provider를 도입하여 외부 시스템 호출의 보상(rollback) 메커니즘을 규격화하고, OCP를 만족하는 구조로 개선했습니다.',
      problem: `
결제가 완료되면 예치금 차감, 외부 제휴 포인트 연동, 배송 요청 등 수십 개의 외부 시스템이 호출되어야 합니다. 기존 코드는 이 모든 과정이 **하나의 거대 메서드**에 분기와 try/catch로 엮여 있었습니다. 

이로 인해 새로운 결제 수단을 추가할 때마다 메서드가 무한정 길어졌고, 특정 단계에서 실패 시 **이전 시스템 호출을 어떻게 롤백할지(보상 트랜잭션) 명확하지 않아** 데이터 정합성에 위험이 있었습니다.
      `.trim(),
      solution: `
세 가지 주요 결정을 통해 구조를 개선했습니다.

1. **Command + Provider 두 단계로 분리**
가장 핵심적인 결정으로, 인터페이스에 \`execute()\`와 \`undo()\`를 같은 위계로 두는 Command 패턴을 택했습니다. Command 자체는 순수하게 실행과 보상만을 책임지며, 객체 생성과 데이터 준비는 Provider에게 위임했습니다.

2. **마커 인터페이스로 주문 유형 분기**
바로드림(매장 픽업)과 일반 주문의 분기를 런타임 if문에서 컴파일 타임 마커 인터페이스로 옮겼습니다. Spring의 의존성 주입을 활용해 새로운 주문 유형이 추가되더라도 기존 Factory 코드를 전혀 수정하지 않도록 구성했습니다.

3. **OpenFeign 마이그레이션**
제각각이던 외부 통신 모듈(RestTemplate 등)을 Spring Cloud OpenFeign으로 일원화하여, 응답 모델의 타입 안전성을 확보하고 RequestContextHolder를 활용해 중복 토큰 발급을 최적화했습니다.
      `.trim(),
      result: `
- 21개 이상의 Command/Provider가 구현되어, 각 비즈니스가 **단일 책임 단위**로 완전히 분리되었습니다.
- **새로운 기능 추가 시 기존 로직 무수정**: 1개의 Command와 1개의 Provider만 추가하면 됩니다.
- 롤백 코드가 인터페이스 단에서 강제되면서 **데이터 정합성 보장**이 강화되었습니다.
      `.trim(),
    },
  },
  {
    slug: 'giftcard-kafka-infrastructure',
    title: '기프트카드 발급 — 결제와 발급 트랜잭션 분리',
    period: '2024.04 - 2024.11',
    tags: ['Kafka', 'Spring Events', 'Outbox Pattern', '비동기'],
    summary:
      '기존 폴링 방식의 15분 지연 발급을 비동기 이벤트 기반으로 개선하여, 결제 즉시 기프트카드가 발급되도록 처리 속도와 일관성을 획기적으로 개선했습니다.',
    sections: {
      summary:
        'Spring의 @TransactionalEventListener(AFTER_COMMIT)와 Kafka를 조합하여 트랜잭션 분리와 이중 쓰기 문제를 해결했습니다. 이벤트 발행 인프라를 처음부터 설계하고 구축하여 타 도메인까지 확장 가능한 기반을 마련했습니다.',
      problem: `
사용자가 기프트카드를 결제하면 내부 결제 처리는 즉시 끝나지만, 정작 카드 번호 발급은 **외부 배치 서버가 15분마다 폴링**하여 진행되는 구조였습니다. 이로 인해 결제 후 카드 번호를 받기까지 큰 지연이 발생했습니다. 

단순히 결제 트랜잭션 내에서 외부 시스템을 동기 호출할 경우, 결제 처리 시간이 길어지고 장애 전파 위험(부분 성공)이 있어 **결제의 일관성과 발급의 즉시성을 동시에 보장할 수단**이 필요했습니다.
      `.trim(),
      solution: `
1. **Spring AFTER_COMMIT 기반 후처리 발행**
이중 쓰기 문제(rollback ghost message)를 방지하기 위해 \`@TransactionalEventListener(phase = TransactionPhase.AFTER_COMMIT)\`를 도입했습니다. 트랜잭션이 성공적으로 커밋된 직후에만 Kafka 발행을 수행하도록 하여 이벤트 유실과 가짜 메시지를 차단했습니다.

2. **비동기 처리로 성능 영향 최소화**
리스너에 \`@Async\`를 적용하여, Kafka 메시지 브로커로의 송신 RTT가 결제 트랜잭션의 응답 시간에 합산되지 않도록 분리했습니다.

3. **공유 EventMessage 제네릭 모델 설계**
확장성을 고려해 단일 봉투 형태의 메시지 구조를 정의했고, 내부의 JSON 페이로드와 Enum 코드로 이벤트를 구분하여 추후 다른 도메인 이벤트도 손쉽게 얹을 수 있도록 설계했습니다.
      `.trim(),
      result: `
- **발급 지연 제거**: 최대 15분 걸리던 발급이 **결제 즉시(0초)** 이루어지게 되었습니다.
- **배치 서버 의존성 제거**: 노후화된 외부 폴링 배치를 종료하여 단일 실패 지점(SPOF)을 제거하고 운영 부담을 줄였습니다.
- 설계한 이벤트 인프라가 배송, 배송지 변경 등 **타 도메인의 표준 비동기 파이프라인으로 정착**되었습니다.
      `.trim(),
    },
  },
  {
    slug: 'order-concurrency-control',
    title: '주문 완료 동시성 제어 — 상태 머신과 비관적 락',
    period: '2026.04',
    tags: ['동시성 제어', 'Pessimistic Lock', 'JPA', '상태 머신'],
    summary:
      '동일 주문 ID로 발생하는 중복 결제 요청 문제를 비관적 락과 상태 머신으로 해결하여, 정상 재시도 흐름은 보장하고 비정상 동시 진입만 직렬화 차단했습니다.',
    sections: {
      summary:
        '단순 차단 방식의 한계를 인지하고 빠르게 v1 코드를 롤백한 뒤, DB 레벨의 비관적 락(SELECT FOR UPDATE)과 다분법 상태 머신 분기를 통해 정상 결제 재시도와 비정상 더블클릭을 완벽하게 구분하는 v2 구조로 재설계했습니다.',
      problem: `
클라이언트 네트워크 재시도나 사용자의 더블클릭으로 인해 같은 주문 ID로 주문 완료 요청이 짧은 시간에 중복 유입되는 문제가 있었습니다. 

처음(v1)에는 단순하게 DB 조회 후 차단하는 식으로 로직을 구성했으나, **READ COMMITTED 격리 수준의 동시성 누수**로 인해 둘 다 통과되거나, 정상적인 **결제 수단 변경 후 재시도마저 차단**되는 운영 이슈가 발생했습니다.
      `.trim(),
      solution: `
단순 조회/차단을 제거(Revert)하고, 두 가지 축으로 아키텍처를 재설계했습니다.

1. **비관적 락(Pessimistic Write)으로 동시성 직렬화**
충돌 빈도가 높은 '더블클릭' 케이스 특성 상 낙관적 락은 적합하지 않았고, 외부 인프라가 필요한 분산 락 대신 DB만으로 해결 가능한 JPA의 \`@Lock(LockModeType.PESSIMISTIC_WRITE)\`을 적용했습니다. 락 범위는 단일 Row로 한정해 성능 저하를 막았습니다.

2. **락 획득 후 다분법 상태 머신 분기**
무조건 차단하는 것이 아니라, 락을 획득한 후 처리 코드를 검사했습니다.
이전 결제가 실패해서 다시 들어온 '정상 재시도'는 통과시키고, '결제 진행 중'이거나 '이미 완료'인 경우만 상태에 맞는 명확한 에러 메시지로 반환하도록 4가지 분기로 세분화했습니다.
      `.trim(),
      result: `
- DB 레벨의 완벽한 직렬화를 통해 **중복 결제로 인한 데이터 오염(보상 운영 건수) 100% 차단**.
- 정상적인 결제 수단 변경 및 재시도 흐름을 복구하여 UX 하락 방지.
- 사용자에게 실패 원인에 맞는 정확한 안내 메시지가 노출되어 관련 CS 문의 감소.
      `.trim(),
    },
  },
  {
    slug: 'auth-unification-aop-cache',
    title: '회원 인증 일원화 및 캐시 적용 — Strangler Fig 기반 점진적 전환',
    period: '2025.11 - 2026.03',
    tags: ['AOP', 'Strangler Fig Pattern', 'Redis', 'Migration'],
    summary:
      '레거시 API에서 신규 API로의 전환을 AOP를 활용해 기존 비즈니스 로직 수정 없이 투명하게 라우팅하고, Redis 캐시를 통해 인증 트래픽 부하를 최적화했습니다.',
    sections: {
      summary:
        '운영 중인 시스템의 중단 없이 회원 인증 체계를 신규 API로 이관하기 위해 Strangler Fig 패턴을 도입했습니다. AOP를 통해 라우팅을 제어하고, 운영 데이터(로그) 기반의 검증을 거쳐 안전하게 트래픽을 넘겼습니다.',
      problem: `
과거에 사용되던 구버전 인증 API를 완전히 종료하고, 전사 통합 신규 인증 API로 모든 시스템을 이관해야 하는 과제가 주어졌습니다.

하지만 기존 주문/결제 코드 수백 곳에 구버전 인증 API 호출이 하드코딩되어 있었고, 이를 일괄적으로 수정하는 것은 **빅뱅 배포의 리스크**가 너무 컸습니다. 결제 과정의 인증 장애는 곧 대규모 매출 손실로 이어지기 때문이었습니다.
      `.trim(),
      solution: `
1. **AOP 기반 투명한 라우팅 제어 (Strangler Fig)**
기존 서비스 코드를 한 줄도 수정하지 않고, 구버전 API 호출 메서드에 AOP Aspect를 적용했습니다. Aspect 내부에서 Feature Flag 역할을 하는 설정 값을 읽어, 점진적으로 신규 API로 트래픽을 우회시켰습니다.

2. **Shadow Traffic 및 데이터 교차 검증**
초기 2주간은 구/신규 API를 동시에 호출하되 결과는 구버전 것을 사용하며, 두 API의 응답 데이터를 비교하는 로그를 남겼습니다. 이 로깅으로 발견한 엣지 케이스들을 수정하며 정합성을 100% 확보했습니다.

3. **Redis 세션 캐싱**
신규 API 서버로 트래픽이 몰릴 것을 대비하여, 결제 세션 내 반복되는 인증 요청은 Spring Cache(@Cacheable)와 Redis를 활용하여 캐싱하여 불필요한 네트워크 I/O를 억제했습니다.
      `.trim(),
      result: `
- 수백 곳의 레거시 코드를 안전하게 격리하고, 단 한 건의 결제 장애 없이 **무중단 마이그레이션** 성공.
- 데이터 교차 검증 덕분에 발생할 뻔한 엣지 케이스 장애를 사전에 차단.
- 인증 API 호출 트래픽이 기존 대비 대폭 감소하여 전사 인프라 비용 절감에 기여.
      `.trim(),
    },
  },
];
